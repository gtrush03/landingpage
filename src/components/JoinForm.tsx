import React from 'react';
import AIStrategyForm from './AIStrategyForm';

const JoinForm: React.FC = () => {
  return (
    <AIStrategyForm
      title="Join TRU SYNTH"
      subtitle="Become part of the AI revolution. Tell us about your goals and we'll create a personalized path to success."
      description="Click 'START' to begin your application."
    />
  );
};

export default JoinForm;
